import { registration, login, checkLogin } from './auth'
import { getCosts, editCosts, createCost } from './costs'

const API = {
  registration,
  login,
  checkLogin,
  getCosts,
  editCosts,
  createCost,
}
export default API
