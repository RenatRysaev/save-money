import { registration, login, checkLogin } from './auth'
import { getCosts, editCosts } from './costs'

const API = {
  registration,
  login,
  checkLogin,
  getCosts,
  editCosts,
}
export default API
